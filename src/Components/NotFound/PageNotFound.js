import React from "react";

const PageNotFound = () => {
  return (
    <div style={{ height: "100vh", marginTop: "100px", color: "brown" }}>
      page not found
    </div>
  );
};

export default PageNotFound;
